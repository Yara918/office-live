---
name: office-live
description: >
  把客户的「飞书多维表格」在 15 分钟内变成一个本地运行的像素办公室互动页面
  （数据实时双向同步）。自动分析表里的人员与事项，娱乐的同时管理团队，让紧张的工作多一份乐趣。
  触发条件：(1) 用户提到"把表格 xxx 变成小剧场 / 办公室页面 / 互动页面"；
  (2) 用户给了一个飞书多维表格链接，需要生成可视化互动页面；
  (3) 用户要"团队成员可视化""办公室小游戏""人员任务管理页面"等。
  覆盖：表格数据分析、必答信息确认、配置生成、本地启动、交付使用指引。
---

# Office Live 小剧场 · Skill

**一句话**：把一个**飞书多维表格**，按正常交付节奏变成一个本地运行的像素办公室互动页面（数据实时双向同步）。全量读取表、字段和样例值，识别可交互的人、事、状态、时间、数值与关联信息，在页面中完成分析展示和必要写回。

**使用方式**：将本 Skill 安装到 coding agent 后，用户提供飞书多维表格链接，agent 按本 Skill 流程完成：读表 → 分析 → 确认 → 生成配置 → 本地启动页面 → 输出本地访问地址。用户仅需：① 提供表格链接 ② 回答几个确认问题 ③ 扫码授权一次（飞书表格读写权限）。

**交付节奏**：正常表格从拿到链接到本地页面可访问，目标控制在 15 分钟内。若超过 15 分钟，继续推进并用简短话术说明当前进度、正在处理的阶段和下一步，不把排查细节持续抛给用户。

---

## 交付主路径

**生成阶段 = 只运行这一条命令，其余全自动：**

```
node scripts/deliver.mjs --base <表格链接或base_token> --admin <管理员姓名> [--code <管理口令>] --task-table <页面主入口表名> [--member-table <人员档案表名>] [--project-table <项目表名>] --owner-field <负责人列名> --name-field <事项名称列名>
```

**主入口不等于全量范围**：`--task-table` 表示页面默认展示、人员工位聚合、负责人调整和新增事项写回的主入口表。客户 Base 里的其他表不能丢弃，脚本会自动加入全量读取、全量画像、概览、人员旅程和详情展示。

**表角色不猜测**：页面主入口表、人员档案表、项目表都来自必答确认环节**用户确认的表名**；没有独立人员表/项目表就不传对应参数（成员从主入口表负责人列聚合）。未被选为主入口/档案/项目的表，统一作为全量数据表参与分析。

**成员同步规则**：页面成员池必须由「全表所有可识别人员关系 + 人员档案表成员 + 管理员」合并去重，并随飞书刷新更新。主入口负责人列只是优先来源之一，项目负责人、参与人、执行人、联系人、销售等出现在其他表中的人员关系也要进入全局统计。地图最多显示 8 人，优先展示有主入口事项或全表相关事项的人；纯档案人员不强行占座，但不能从数据分析和负责人候选中丢失。按负责人筛选必须兼容多人负责人，不能只做字符串完全相等。

**管理员不新增重复人**：管理员是权限角色，不是额外业务成员。若管理员姓名已经能和负责人列或人员档案表中的成员唯一匹配（如成员名带部门前缀），页面应复用该成员并标记权限，不再新增一个 0 数据管理员。

**脚本自动完成**：读取表格 → 生成配置 → 本地模式准备 → 验证数据 → 输出本地启动指引。

**交付闭环**：生成、安装依赖、启动服务、验证首页、验证 `/api/office/snapshot?force=1` 属于同一条交付链路。**启动服务必须用「系统托管」方式（Windows：计划任务 `schtasks`；macOS：`launchd`），禁止把裸 `npm run dev` 后台进程当作交付**——部分 agent 平台会自动回收 agent 会话启动的后台进程，导致「交付的链接打不开」。系统托管方式由操作系统管理进程、不依赖 agent 会话，交付后链接持续存活。启动后必须验证首页 200 与快照 `source: feishu`，再一次性给出本地地址、数据来源、表数、记录数、成员数和写回验证结论。除非需要用户扫码授权、等待用户补齐必答确认，或命令已经明确失败，否则不要停在“装依赖中 / 服务启动中 / 验证中”。详细见 [references/deployment.md](references/deployment.md)「启动页面」与 [references/operations.md](references/operations.md) 第 2 节。

**授权不能后台等待（一次扫码即可，必须完成令牌兑换）**：首次未授权时，`deliver.mjs` 生成飞书授权二维码/链接并输出 `device_code` 后退出。用户扫码同意后，agent 必须先用 `deliver.mjs` 输出的 `device_code` 执行**令牌兑换**（`lark-cli auth login --device-code <code> --domain base`，该命令会阻塞直到授权完成并落盘），再运行 `lark-cli auth status --json --verify` 确认授权生效，最后重跑同一条 `deliver.mjs` 命令。**只做 `auth status` 而不做 `--device-code` 兑换，令牌不会落盘，会错误地要求用户再扫一次码**。不要在同一条命令里长时间轮询 `auth login --device-code`（`--device-code` 本身是阻塞兑换命令，只能单独执行），不要对用户说“授权后我会自动继续”。

**授权检查必须真正生效，绝不静默跳过**：授权命令必须使用**支持 `auth` 子命令的 lark-cli**（npm 版 `lark-cli`/`lark-cli.cmd`）。若 lark-cli 报 `unknown command "auth"`，绝不能跳过授权检查，必须改用支持 `auth` 的 lark-cli，或改用“读表实测”探测授权（跑一条 `base +table-list`，能读到数据=已授权；报 `temporary token expired`/未授权=需重新扫码）。用户主动登出或 token 过期后，页面会降级为只剩管理员，此时必须用 `lark-cli auth login --domain base` 触发扫码重新授权，再继续。详细见 [references/operations.md](references/operations.md) 第 1、2 节。

**端口不能假设**：本地页面优先使用 3000；若 3000 已被旧服务占用，必须自动换 3001/3002 等可用端口。启动、首页验证、快照验证和最终回复必须使用同一个真实端口，不能误连旧服务。

**输出目录不能污染 Skill（任何 agent/安装位置都适用）**：生成产物**默认统一写到用户文档目录** `Documents\office-live-output`，或用环境变量 `OFFICE_LIVE_OUTPUT_DIR` 显式覆盖；**不依赖 skill 装在哪个路径、不依赖用户用哪个 agent**。禁止在任何 skill 安装目录、`.skills`、`.user_skills`、仓库根目录或其他 agent 目录内残留 `office-live-output`、`node_modules`、`.next`、缓存或临时授权文件（否则会导致 skill 目录膨胀、位置错乱）。详细见 [references/operations.md](references/operations.md) 第 4 节。

**搜索范围边界**：排查和安装只允许查当前项目目录、本 Skill 安装目录、输出目录、用户明确给出的目录。不要运行全盘搜索或系统盘递归搜索；找不到文件就报具体路径缺失，不扩大到整机搜索。

**生成边界**：优先让 `deliver.mjs` 完成读表、生成配置和本地准备。agent 可以判断未知问题，但已知问题按本 Skill 的排错路标处理；不要从零开始全盘搜索，不把 `.ts` 配置文件当成交付成果，不手动改动与本次生成无关的业务逻辑。

**对用户汇报**：正常进度只报阶段和结果，例如“已完成读表，正在启动页面验证”。遇到问题时只报结论、影响和下一步；内部命令、反复试错和长日志不持续发给用户。若确实无法继续，再给出真实错误和需要用户处理的动作。

> 授权：若 lark-cli 未登录飞书表格读写权限，脚本会输出授权二维码/链接和 `device_code` 并退出；用户扫码同意后，agent 先执行 `lark-cli auth login --device-code <code> --domain base` 完成令牌兑换，再验证授权状态，最后重跑同一条生成命令继续交付。全程不需要任何应用凭证。

---

## 交付物

最终产物 = **本地可运行的网页**（页面运行在用户 agent 环境中，通过 lark-cli 以用户身份读写飞书表格）：

- 🏢 像素办公室，成员在工位走动互动（数据来自飞书表格，实时同步）
- 👤 点成员看工作旅程 / 任务与项目情况
- 🔄 管理员可转任务 / 新增任务（双向写回飞书）
- 🎮 互动玩法（专注/会议/休息/学习等），全员可玩

**表格权限说明（写回闸门：分享权限为「可编辑」）**：
- 表格分享权限为「互联网上获得链接的人可编辑」→ 页面与表格双向同步全通
- 未开启或仅「可阅读」→ 一律只读：页面可查看与互动，写回被拦下（无论账号是谁，含表主本人）

---

## 触发识别（何时激活本 Skill）

用户说以下内容即触发（识别关键词：**表格 + 小剧场/办公室/互动/可视化/成员管理**）：

| 用户可能这样说 | 判定 |
|---|---|
| "请读我的飞书表格：<链接>，生成 Office Live 小剧场" | ✅ 激活 |
| "把「<表格名>」做成像素办公室互动页面" | ✅ 激活 |
| "把我的成员任务表变成一个能互动、能管理任务的办公室页面" | ✅ 激活 |
| "帮我做个项目看板"（无表格/无互动意图） | ❌ 不激活 |

**拿到需求后立即**：让用户提供表格链接（若未给）→ 进入生成流程。

> 用户触发话术与对话示例：见 [README.md](README.md)「使用方法」与 [examples/conversation-example.md](examples/conversation-example.md)

---

## 流程速览（详细见 references/delivery-flow.md）

| 步骤 | 预算 | 关键动作 |
|---|---|---|
| 1. 表格数据分析 | 2 min | 读全部表 + 字段 + 数据，形成全量表画像，并推荐一个页面主入口表 |
| 2. 必答确认 | 1 min | 一次反馈：全量数据概况 + 主入口/可选档案表确认 + 4 必答（事项名称列/负责人列/管理员/8人限制）+ 口令说明 |
| 3. 生成配置 | 1 min | 只改 `lib/office-config.ts`（见 references/config-guide.md） |
| 4. 本地启动准备 | 2-4 min | 清空前缀、验证数据（见 references/deployment.md） |
| 5. 验证交付 | 1 min | 数据读取正确 + 输出本地启动指引 |
| **合计** | **≤ 15 min** | — |

**核心规则**：
- **表有什么就分析什么（最高原则）**：全面读取客户 Base 里的所有表、所有字段、字段类型、样例值、数据量和关联关系，逐字段判断通用能力（人、事项、状态、日期、数值、比例、关联、附件、只读/可写）。**读取、展示、分析不因任何字段类型受限**：文本、数字、日期、人员、关联、lookup、公式、自动编号、创建/更新时间、附件等都要读到并转成可理解内容。**不预设行业、不套固定模板、不丢弃未选表**——能识别就分析，识别不稳就保留原字段展示，不硬算结论。本原则适用于各类客户、各类场景和不同结构的表。
- **主入口由用户确认，全量表继续分析**：页面主入口表、人员档案表、项目表由**用户确认**后传入（`--task-table/--member-table/--project-table`）。`--task-table` 只是人+事交互和写回入口；其他表自动作为 `data` 表进入全量分析和详情展示。
- **成员必须随表同步**：成员池来自全表所有可识别人员关系、人员档案表和管理员的合并去重；主入口负责人列只是默认交互入口，不代表全量人员范围。人员档案表新增的人即使当前没有事项，也要进入数据分析和调整候选；地图优先展示有主入口事项或全表相关事项的人，纯档案人员不强行占座。地图只受 8 人上限影响，不代表数据被丢弃。
- **管理员不能凭空多一人**：管理员只作为权限角色进入页面；如果已存在唯一匹配成员，复用该成员，不新增一个 0 记录人员。
- **展示层必须降噪但不能丢字段**：页面可以全量读取，但不能全量摊开。概览和列表只展示业务可读内容，其余进入折叠详情或只读区；隐藏字段数量、字段类型、人/状态/数值/关联等技术分类词。关联记录 ID、日期序列号、百分比小数、对象数组等必须先格式化，解析不出可读名称时显示“已关联 N 条”或放入折叠详情，绝不把 `rec...` 之类内部 ID 露给用户。
- **日期分析必须保守**：所有日期字段都要读取和格式化，但不能把早于今天的日期一律说成“已过期”。只有字段语义和表上下文明确指向期限、到期、下次行动、提醒、续约、复查，或计划完成/预计交付/应回款等承诺节点时，才计算“已过期/今日/7天内”；入职、离职、出生、成立、创建、更新、建档、登记、签约、生效、开始、实际发生、完成、上线、归档等历史事实日期，以及语义不确定的日期，只展示范围或分布，不输出风险结论。
- **数值/完成度分析必须取表值（不预设、不硬算）**：表里有现成的「进度/完成率/完成度」等比例字段时，必须取表格原值做汇总（多记录平均），**不得**自动引入金额/数量/预算等无关字段做加权（它们与完成度无因果关联，会得到不可信的数值）。表格没有现成比例字段、需要自行计算时，只能用与进度真正关联的字段（如已完成/已结束记录数 ÷ 总记录数）；算不出语义合理的数值时，保留原字段/原值展示，不编造结论。本规则适用于任何客户表、任何字段名，不针对特定字段。
- **同步失败先稳住体验**：飞书细粒度限流要退避重试；首次同步中显示“正在同步/正在连接”，不要把短暂重试误报成故障。只有真实读取失败才提示用户检查授权或权限。
- **页面同步必须实时取新（8 秒轮询保留）**：8 秒轮询是客户明确要求，**必须保留**（飞书改动 8 秒内反映到页面）。但**轮询请求走缓存（不带 force）**，命中后端 8 秒 TTL 缓存，避免每次轮询都全量重读飞书打限流；只有页面初始化、窗口重新可见、手动刷新、写回前后才 `force=1` 强制读取最新快照。权限从可阅读改成可编辑后，下一次写回必须重新检查权限，不能继续沿用旧权限缓存。
- **写回验证避免中文乱码**：新增/调整负责人等写回实测必须使用 UTF-8 JSON body 或临时 JSON 文件提交；不要在 shell 命令里直接拼接中文姓名、字段值或口令，避免编码导致误判。
- **必答确认关卡**：用户必须确认完页面主入口表、可选人员档案表/项目表与 4 项（**①事项名称列：请用户写出那一列的列名 ②负责人列：请用户写出那一列的列名 ③管理员 ④8人限制**——完全按用户写的列名使用，不做任何映射/猜测）才生成，未确认不生成
- **读写分层规则（必读）**：任何字段都必须读取、格式化、展示或折叠展示；只有写回时才判断可写性。负责人列可以是可写人员字段、关联字段或普通文本字段；页面读取成员时不因字段类型丢人。lookup、公式、自动编号、创建/更新时间、创建/修改人等只读列必须展示，但写回时跳过或提示“此字段由飞书自动生成或计算，不能手动写入”，不能阻断其他字段和主流程。
- **新建事项按真实字段写回**：事项名称列、负责人列、状态列、日期列、关联列、下拉列、数字列都不能假设成文本。新建表单必须根据字段元数据渲染控件，并以 `真实字段名 -> 值` 提交；后端按字段真实类型转换和逐字段读回校验。若事项名称列本身是关联/下拉/日期/数字字段，也必须按该字段类型写回，不能另走文本标题捷径。
- **生成由 `deliver.mjs` 处理**：脚本自动完成"读表→生成配置→本地模式准备→验证→输出启动指引"。不要手动改配置、手动挑应用、手动初始化 git / 复制模板 / 手动推送；脚本明确失败时，按错误类型走排错路标。
- **启动验证要跑到结论**：`npm install`、`npm run dev`、首页验证和快照验证没有完成前，不把“装依赖中/待起服务/待验证”作为最终交付。需要用户授权、必答确认或外部权限处理时可以暂停，并说明需要用户做什么。
- **页面读取失败先看真实原因**：若页面没有读到飞书数据，先看服务端日志和快照接口真实错误。未授权就回到生成阶段运行 `deliver.mjs` 完成扫码授权；飞书限流就退避重试；`deliver.mjs` 能读而页面不能读时，优先检查页面服务是否继承同一套 lark 环境。
- **绝不要向用户要凭证**：授权全部通过自动弹出的扫码链接完成（飞书表格读写），**不要向用户索要任何应用 ID、密钥、口令以外的值**。
- **无需任何预配凭证**：页面通过 lark-cli 以用户扫码授权后的身份读写表格——不需要任何应用凭证、不需要部署到任何平台。**不要以"缺少凭证"为由暂停**——直接执行，授权链接会自动弹出。
- **带结论提问**：先分析数据，带着具体数字（N 人/M 任务/W 超期）去问用户
- **人数限制**：最多 8 人（含管理员），面向 8 人以内小团队，超限不显示
- **管理口令**：所有改动表格的操作需口令（默认取表格名）；互动和查看全员可用

---

## 前置条件

| 条件 | 说明 |
|---|---|
| lark-cli | 需已安装并支持飞书表格读写授权（`lark-cli auth login --domain base`）。**安装后必须先检测是否支持 `auth` 子命令**：执行 `lark-cli auth status`，若报 `unknown command "auth"`，说明当前 lark-cli 不支持授权，必须改用支持 `auth` 的 npm 版 lark-cli（全局命令 `lark-cli` / `lark-cli.cmd`）后重试，**绝不能跳过授权检查**。首次生成时自动弹出授权链接，用户扫码一次 |
| 页面模板 | 本 Skill 自带 `templates/page/`（完整 Next.js 项目） |
| Node.js | 本地运行页面所需（`npm run dev` / `npm run build`） |
| 表格可访问 | 用户提供表格链接；生成阶段只需可读，编辑权限可选（影响写回能力） |

---

## 安装 Skill（给 agent 的执行说明）

1. 让用户提供本 Skill 的仓库地址，用 `git clone` 安装（或按平台 Skill 安装流程导入）。
2. **`git clone` 失败（TLS 握手 / 网络代理不通）时，自动降级为下载 zip 压缩包安装**：下载仓库 zip → 解压 → 放到 Skill 安装目录（内容与 git 版本一致）。不要因 git 不通就卡死或放弃。
3. 安装完成后，**先检测 lark-cli 是否支持 `auth`**（见上表），不支持就改用 npm 版 lark-cli，再进入读表流程。
4. 安装前如已有同名旧副本，先删除旧副本再安装，保证装的是干净最新版。

---

## 交付须知（必须告知用户）

1. **人数限制**：最多展示 8 人（含管理员），超出不显示
2. **管理员**：确认表格维护人，作为小剧场管理员（可分配任务/调整负责人）
3. **口令**：管理员操作需口令，默认取表格名称
4. **表格权限**：生成阶段表格可读即可；写回操作（调整负责人/新增任务等）要求表格分享权限为**「互联网上获得链接的人可编辑」**——未开启或仅「可阅读」时一律只读（谁都不例外，含表主本人）。
5. **适用表**：各类客户 Base 都会全量分析展示；其中带有明确“人 + 事”字段的表，可作为页面主入口支持工位聚合、负责人调整和新增事项写回。
6. **数据体量**：建议 500 条以内，这个范围内页面加载和实时同步最流畅。如果表格达到数千条，通常是进销存或工单系统这类大场景，更建议在飞书原表里管理——本产品更适合轻量、直观的团队协作。

> ⚠️ **要表话术（必说）**：请用户分享表格链接时确认可读；如需写回操作，说明"生成页面只需可读；如需在页面写回，请把表格分享权限设为 **「互联网上获得链接的人可编辑」**——未开启或仅可阅读时，页面只能查看与互动（写回被拦下）"。

> 完整说明模板：见 references/delivery-flow.md

---

## 参考文档

- [交付流程详解](references/delivery-flow.md) —— 数据分析 / 话术 / 确认 / 生成 / 验证
- [配置生成指南](references/config-guide.md) —— 字段映射规则
- [本地启动指南](references/deployment.md) —— 本地启动 / 验证 / 排错
- [给 Agent 的操作指引](references/operations.md) —— **接手前必读**：授权/连不上排查/8 秒轮询/输出目录/敏感信息
- [常见问题](references/faq.md) —— 交付 / 技术 / 访问 / 权限问题
- [交付规则](RULES.md) —— 交付标准 / 确认关卡 / 人数限制 / 合规边界
- [配置示例](templates/office-config.example.ts)
- [README（对外介绍）](README.md)
