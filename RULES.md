# Office Live · 交付规则

## 生成标准

- **目标：从拿到表格到可运行 ≤ 15 分钟**（理想 15 分钟）。
- **超过 15 分钟也继续推进**：用简短话术汇报当前阶段和下一步，不把排查细节持续发给用户。
- **交付闭环跑到结论**：生成配置、安装依赖、启动服务、验证首页、验证 `/api/office/snapshot?force=1` 是同一条链路。除非等待用户扫码授权、等待用户补齐必答确认，或命令明确失败，否则不要把“装依赖中 / 服务启动中 / 验证中”作为最终交付。
- **成功交付必须有证据**：最终回复必须包含本地访问地址、首页可访问结果、快照 `source: feishu`、表数/记录数/成员数，以及写回验证成功或被权限拦下的原因。
- **端口必须真实可用**：优先使用 3000；若 3000 已被旧服务占用，必须自动换 3001/3002 等可用端口。启动、验证和最终回复必须使用同一个真实端口，不能误连残留服务。
- **交付目录不得污染 Skill**：任何路径只要形如 `*/skills/office-live`，都视为已安装 Skill，生成产物必须写到用户文档目录下的独立 `office-live-output`，或用户显式指定的 `OFFICE_LIVE_OUTPUT_DIR`。不得在 Skill 安装目录内留下 `office-live-output`、`node_modules`、`.next`、缓存、授权二维码或临时工具副本。
- **搜索范围边界**：只查当前项目目录、本 Skill 安装目录、输出目录、用户明确给出的目录。不要运行全盘搜索或系统盘递归搜索；找不到文件就报具体路径缺失。
- **中文写回验证必须 UTF-8**：新增任务、调整负责人等验证请求必须用 UTF-8 JSON body 或临时 JSON 文件提交，不能在 shell 里直接拼接中文造成乱码误判。

## 生成规则（本地模式，零平台依赖）

- 页面运行在用户 agent 环境（本地），通过 lark-cli 以用户扫码授权后的身份读写飞书表格。
- **不部署到任何外部平台、不创建任何应用、不需要任何应用凭证/密钥**——全程无平台资产。
- **不要以"缺少凭证"为由暂停**：授权通过自动弹出的扫码链接完成，一次授权、自动续期。
- **页面读取失败要先看真实原因**：若页面没有读到飞书数据，先看服务端日志和快照接口真实错误。未授权就回到生成阶段运行 `deliver.mjs` 完成扫码授权；飞书限流就退避重试；若 `deliver.mjs` 能读而页面不能读，优先检查页面服务是否继承同一套 lark 环境。
- **飞书限流自动处理**：遇到 `800050828` 或 fine-grained rate limiting 时，执行环境必须等待退避后自动重试；不得让用户手动等待、手动重跑，也不得把瞬时限流直接当作真实失败。
- **lark-cli 路径必须统一**：读表、生成、页面服务和写回必须使用同一套 `LARK_CLI_BIN/LARK_CLI_PATH` 与授权环境；可执行文件路径含空格时脚本必须自动加引号，不得复制 exe 副本绕过。

## 必答确认关卡（不可跳过）

- 用户必须回答完所有必答项并**明确确认**，才允许生成配置。
- 必答项：① **页面主入口表 / 可选人员档案表 / 可选项目表确认（Agent 先读完全部表并给出推荐，用户确认或改选；未选表仍纳入全量分析）** ② **事项名称字段（请用户写出那一列的字段名，完全按用户写的列名使用，不做任何映射/猜测）** ③ **负责人字段（请用户写出那一列的字段名，完全按用户写的列名使用，不做任何映射/猜测）** ④ 管理员姓名 ⑤ 接受 8 人限制。
- **负责人列不强制字段类型**：可写人员字段、关联字段、普通文本字段都可作为负责人列；读取成员时不因字段类型丢人。lookup、公式、自动编号等只读计算列也要读取展示，但不能作为写回目标；若用户确认了只读列，页面必须明确提示无法写回原因，而不是丢字段或静默失败。
- **关键列不强制文本类型**：事项名称列、负责人列、状态列、日期列、关联列、下拉列、数字列都必须按飞书真实字段类型读取、渲染、写回和读回校验。新建事项使用 `真实字段名 -> 值` 的全字段提交；不能把事项名称列固定当文本标题，也不能因为字段是关联/下拉/日期/数字就从表单里绕开。
- **主入口由确认决定**：`--task-table / --member-table / --project-table` 来自用户确认的表名；`--task-table` 是页面主入口和写回入口，不代表全量分析范围。没有独立人员表/项目表时对应参数不传（成员自动从主入口表负责人列聚合）。
- **成员同步不丢人**：成员池必须由全表所有可识别人员关系、人员档案表和管理员合并去重，并随飞书刷新更新。主入口负责人列只是默认交互入口，不代表全量人员范围；项目负责人、参与人、执行人、联系人、销售等出现在其他表中的人员关系也要进入全局统计。地图最多显示 8 人，优先展示有主入口事项或全表相关事项的人；纯档案人员不强行占座，超出人员仍进入数据分析和负责人调整候选。按负责人筛选必须兼容多人负责人。
- **管理员不重复造人**：管理员是权限角色。若管理员姓名与已有成员唯一匹配，复用该成员；不要新增一个没有记录的重复管理员。
- **未确认完，不进入生成。**

## 进度汇报

- 正常进度只汇报阶段和结果，例如“已完成读表，正在启动页面验证”。
- 遇到已知问题时只汇报结论、影响和下一步，例如“3000 被占用，已改用 3001 验证”。
- 不连续发送内部命令、反复试错过程和长日志给用户；只有无法继续或需要用户动作时，才给真实错误和处理建议。

## 人数限制

- 页面最多展示 **8 人（含管理员）**，超过 8 人成员不显示。
- 8 人限制只限制地图工位和角色素材，不允许影响全量数据分析和负责人调整候选。
- 生成前主动核实人数，超过 8 人**提前告知用户**，不要等用户发现。
- 本 Skill 面向 **8 人以内小团队**，不提供自动扩容。

## 管理口令

- 所有"改动表格"的操作（点 E / 调整负责人 / 新增任务 / 转任务）需要管理口令。
- 口令默认取**表格名称**（话术中告知管理员）。
- 互动玩法、查看档案：全员可用，无需口令。
- 口令解锁为会话级；刷新页面 / 新设备需重新输入。

## 合规红线

- **全文不得出现**：真实人名、公司名、客户名、飞书应用密钥、任何可识别个人的信息。
- 文档中所有示例用占位符：`<管理员姓名>`、`<表格名>`、`<base_token>`。
- 页面左上角显示**用户自己的表格名**（不加任何标记）。
- 交付时不给用户代码 / 密钥 / 内部配置。

## 适配原则（表有什么就分析什么——最高原则）

- **不预设任何行业、不套模板**：全面读取表格的所有表、所有字段、字段类型、样例值、数据量和关联关系，逐字段判断通用能力（人、事项、状态、日期、数值、比例、关联、附件、只读/可写），并推荐一个页面主入口表。读取、展示、分析不因任何字段类型受限。
- **主入口由用户确认，未选表不丢弃**：Agent 的推荐仅作参考，页面主入口表/人员档案表/项目表以用户确认为准；其他表自动作为全量数据表参与分析、概览、人员旅程和详情展示。
- 表复杂的就分析多，简单的就分析少；识别不了的字段不硬套，页面保留原字段展示，不报错。
- 新建/调整表单必须全字段可见：可写字段提供输入控件；不可写字段置灰展示并说明原因（如“此字段由飞书自动生成或计算，不能手动写入”）。不能因为某一列不可写就隐藏该列，也不能阻断整条记录保存。
- 页面展示必须先格式化再露出：日期序列号转日期、百分比小数转百分比、人员转姓名、关联记录转标题；解析不到的关联记录只显示“已关联 N 条”或放入折叠详情，绝不展示内部 record ID。
- 日期字段必须按语义展示：所有日期都读取，但只有明确表示期限、到期、下次行动、提醒、续约、复查，或计划完成/预计交付/应回款等承诺节点的日期，才计算“已过期/今日/7天内”；历史事实日期或语义不确定的日期只展示范围或分布，不能制造风险结论。
- 概览和列表只露少量重点信息，其余内容默认折叠；不要在用户界面展示字段数量、字段类型或技术分类词。
- 适用于各类客户、各类场景和不同结构的表。
- 只对影响产品逻辑的问题（管理员是谁 / 负责人字段 / 页面主入口表）才问用户，其余自动适配。
- 缺表缺字段不阻塞生成：页面自动隐藏对应区域。

## 表格权限（写回闸门：分享权限为「可编辑」）

- **生成阶段**：表格可读即可——任何权限形态都能分析并生成页面（读取数据不依赖编辑权限）。
- **写回判据 = 表格分享权限是否为「互联网上获得链接的人可编辑」**：是 → 双向同步全通；否（未开启 / 仅可阅读）→ **一律只读，只能查看与互动，写回被拦下——无论账号是谁，含表主本人**。
- **要表时必须明示**：向用户要链接时说明"生成页面只需可读；如需在页面写回（调整负责人/新增任务），请把表格分享权限设为「互联网上获得链接的人可编辑」；未开启或仅可阅读时，页面只能查看与互动"。
- **验证写回**：验证阶段必须实际测一次写回（如新增一条任务再删除），确认表格可写；不可写时明确告知用户"当前表格可能是只读/可阅读权限，未开启编辑权限。请在表格右上角「分享」中，把权限改为「互联网上获得链接的人可编辑」，保存后刷新页面，即可使用写回功能"。
- **权限实时判断**：每次写回前必须重新读取当前分享权限；用户从可阅读改成可编辑后，下一次写回不能继续使用旧权限缓存。

## 数据同步

- **页面 8 秒同步必须真实取新**：页面初始化、窗口重新可见、手动刷新、自动轮询、写回前后，都必须用最新快照校验飞书数据。不得长期返回旧磁盘快照或静态数据。
- **本地缓存只允许短内存缓存**：服务端可保留最多 8 秒的内存快照用于抗抖，但不得从磁盘快照恢复旧数据作为实时页面结果。
- **写回后必须连续确认**：写回成功后立即刷新，并在短时间内再次强制刷新，避免飞书最终一致延迟造成页面停在旧值。
- **错误话术必须透出真实原因**：权限不可写、字段只读、人员 ID 缺失、关联 ID 缺失、选项不存在、授权失效必须分别说明，不得统一提示“检查授权或字段格式”。
